struct Display();

impl Display {
    fn clear_screen(&self) {
        unimplemented!();
    }

    fn draw_sprite(&self, coordinate: (u8, u8), data: [u8; 15], data_len: u8) -> bool {
        unimplemented!();
    }
}
